apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend:5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend:5
  template:
    metadata:
      labels:
        app: frontend:5
    spec:
      containers:
        - name: frontend:5
          image: kanwarsaad/frontend:5
          env:
            - name: REACT_APP_API_URL
              value: "http://backend-service:80"
          ports:
            - containerPort: 3000
