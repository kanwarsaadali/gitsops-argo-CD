apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend:4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend:4
  template:
    metadata:
      labels:
        app: frontend:4
    spec:
      containers:
        - name: frontend:4
          image: kanwarsaad/frontend:4
          env:
            - name: REACT_APP_API_URL
              value: "http://backend-service:80"
          ports:
            - containerPort: 3000
