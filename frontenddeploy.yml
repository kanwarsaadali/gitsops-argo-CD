apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend:37
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend:37
  template:
    metadata:
      labels:
        app: frontend:37
    spec:
      containers:
        - name: frontend:37
          image: kanwarsaad/frontend:39
          env:
            - name: REACT_APP_API_URL
              value: "http://backend-service:80"
          ports:
            - containerPort: 3000

